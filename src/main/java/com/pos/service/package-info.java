/**
 * Business logic. Owns {@code @Transactional} and throws domain exceptions from
 * {@code com.pos.exception} — never anything HTTP-shaped.
 *
 * <p>Populated from C3 onwards.
 */
package com.pos.service;
