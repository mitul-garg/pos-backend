# Bugs Identified & Fixed — human-testing log

A running log of bugs and UX issues found by **manual human testing** of the running
app (things that compiled and passed lint/build but broke or felt wrong when a
person actually used the UI). Covers both **frontend and backend** as the project
grows. This complements `requirements.md` (the spec) — it records what real usage
surfaced that the spec/automated checks didn't.

## Routine for agents

- **When a human reports a bug during testing, add a row here** (newest at the top of the table), then fix it.
- Update the row's **Status** when it's resolved (`Fixed`, `By design`, `Won't fix`, `Open`, `Deferred`).
- Set **Area** to `Frontend` or `Backend`, and **Phase** to the build step it surfaced in (`7`, `8 (B4)`, `C3`, …).
- If the fix or decision changes a spec decision, also update `requirements.md` **and** the relevant `frontend/prompts/` or `backend/prompts/` doc in the same change — this file is a log, not a substitute for the spec.
- Keep entries terse: what the human saw, and how it was resolved. Link files with normal relative paths.
- **Backend bugs found by a failing test rather than by a human don't belong here** — fix them and let the test stand as the record. This file is specifically for what real usage surfaced that the automated checks didn't.
- **Things caught during review go in [Code smells fixed](#code-smells-fixed) instead**, not the log above — a compiler or IDE warning, duplication, an inconsistency. Nothing was broken at runtime, so there's no "what the human saw"; what's worth recording is the pattern, so the next project pre-empts it.

## Log

| # | Date | Area | Phase | What the human saw | Status | Resolution |
|---|------|------|-------|--------------------|--------|------------|
| 15 | 2026-08-06 | Backend | C3 (found in C8) | `POST /api/auth/login` answered 401 with correct credentials whenever the client happened to send a stale/expired `Authorization` header alongside the login request. | Fixed | `JwtAuthenticationFilter` runs in front of `authorizeHttpRequests` (`addFilterBefore`) and processed *any* present Bearer token unconditionally, before the chain ever got to evaluate that `/api/auth/login` is `permitAll()` — an unparseable/expired token tripped `catch (InvalidCredentialsException ex)` and wrote the 401 directly, so the request never reached `AuthController`/`AuthService` at all. The class's own Javadoc only reasoned about a *missing* header ("that is what lets ... `POST /api/auth/login` stay open"), not a present-but-unusable one. Both the filter's own 401 and `AuthService`'s are byte-identical by design (`AuthControllerIT`), so the symptom looked exactly like a credentials/seeding problem — cost real time to isolate. Fixed by threading `SecurityConfig`'s public-path list into the filter as one `OrRequestMatcher` (`publicPathMatcher()`), checked before the token is even read — a public path now skips token processing outright, the same as a missing header. `JwtAuthenticationFilter.java`, `SecurityConfig.java`; regression case in `JwtAuthenticationFilterTest.publicPathIsSkippedRegardlessOfTheToken`. |
| 14 | 2026-08-02 | Frontend | 8 (B6) | Logging out of one tenant and immediately signing in as another landed on the *previous* tenant's last route instead of Checkout. | Fixed | `ProtectedRoute` stashed the attempted route as `state.from` and `Login` restored it — fine for a session-expiry resume, wrong across a login boundary, where it drops you on the previous tenant's screen (and for a deep link like `/receipt/:id`, on a URL that now correctly 404s). Login now always goes to `homePathForRole(role)`; `ProtectedRoute` no longer carries `from` (dead state that would read as if it still worked). A login is a fresh session, not a resumption. `Login.jsx`, `ProtectedRoute.jsx`. |
| 13 | 2026-08-02 | Frontend | 8 (B6) | Printed receipts still showed the placeholder "PoS Store" instead of the tenant's store name. | Fixed | The header markup was duplicated in `ReceiptDocument` and `CreditNoteDocument` — which is *why* both were wrong, and why fixing one would have left the other stale. Extracted `components/PrintDocumentHeader.jsx` taking `{ storeName, subtitle }`; `Receipt.jsx` / `CreditNoteView.jsx` pass the signed-in tenant's `tenantName` (orders and returns are tenant-scoped, so it's always the document's own store). Also folded in a 5th copy of `METHOD_LABELS` in `ReceiptDocument` that smell C1 missed — now `PAYMENT_METHOD_LABELS`. |
| 12 | 2026-08-02 | Frontend | 8 (B5) | On the Tenants list, a suspended tenant's **Reactivate** button looked greyed out / disabled even though it worked. | Fixed | The row carried `opacity-60`, and CSS `opacity` composites the element as a group — a child **cannot** opt back out, so the dimming intended for "inactive record" landed on the one control that most needs to look clickable. Replaced with a `bg-gray-50` tint plus greyed *text cells*, leaving the action at full contrast; the red "Suspended" badge still carries the state. Applied to `UserRow` too, which had the identical problem on its Reactivate button. **Rule: tint inactive rows, never `opacity-` them.** `TenantRow.jsx`, `UserRow.jsx`. |
| 11 | 2026-08-02 | Frontend | 8 (B4) | Signed in as the platform `superadmin`: Order history and Return history showed the expected "This action requires a store account" error, but Checkout and Returns showed a normal (empty) screen with no error. | Fixed | Symptom of relying on the *service layer* to keep a SUPER_ADMIN off POS screens: Orders/ReturnHistory fetch on mount so `requireTenantId()` threw immediately, while Checkout/Returns only call a service on interaction (scan, order lookup) so nothing threw. requirements.md §13.2 asks for those screens to be **unavailable, not empty** — fixed in **B5** by gating the POS routes with `allowedRoles={[ADMIN, CASHIER]}`, so a SUPER_ADMIN is redirected to `/tenants` and never renders them at all. The service-level guard stays as belt-and-braces. `AppRoutes.jsx`, `Layout.jsx`. |
| 10 | 2026-08-01 | Frontend | 7 | A deactivated user trying to log in got the generic "Invalid username or password" instead of being told the account is deactivated. | Fixed | `authService.login` now splits the checks: wrong username/password → generic message (the backend's **401**); correct credentials but `isActive === false` → "This account has been deactivated…" (the backend's **403**). The deactivation message only ever reaches someone who already supplied the right password, so it doesn't leak account existence. `authService.js`; requirements.md §9 notes the 401/403 mapping. |
| 9 | 2026-08-01 | Frontend | 7 | Hold an order → resume it → "Clear cart" → the order was still listed as HELD in Order history and could be resumed again, even though it had been explicitly discarded. | Fixed | Resuming (`loadOrder`) only copies the order into the cart; "Clear cart" emptied the cart + detached `activeOrderId` but never touched the stored order. `handleClearCart` now cancels the resumed order (`status: CANCELLED`, behind a `window.confirm`) when `activeOrderId` is set, so a discarded parked sale drops out of the HELD list. A freshly-held order that was never resumed has no `activeOrderId` and is untouched. `Checkout.jsx`. |
| 8 | 2026-07-18 | Frontend | 4 | Removing the phantom cart line for an out-of-stock item didn't clear the lingering "out of stock" warning. | Fixed | Consequence of #7 — stock-0 items no longer create a line, so there's no phantom line to remove and no orphaned warning. `Checkout.jsx`. |
| 7 | 2026-07-18 | Frontend | 4 | Adding a fully out-of-stock item (`POS-QR-000006`) still created a cart row (qty 1) with an orange "only 0 in stock" line. | Fixed | `handleScan` now short-circuits `stockQuantity === 0` with an "out of stock — not added" warning instead of adding a line — immediate operator feedback. In-stock over-quantity edits still warn-not-block. `requirements.md` §10 updated. |
| 6 | 2026-07-18 | Frontend | 4 | Green "added" confirmation never auto-cleared — sat there stale until the next scan/clear. | Fixed | Kind-aware auto-dismiss: `success` fades after 2 s; `error`/`warning` persist until the next scan (a failed scan must not silently vanish). `Checkout.jsx`. |
| 5 | 2026-07-18 | Frontend | 4 | Clearing the cart left the last green "added" notification on screen. | Fixed | `handleClearCart` also resets `status` to `null`. `Checkout.jsx`. |
| 4 | 2026-07-16 | Frontend | 4 | Camera kept re-scanning and re-adding the same product every ~1 s while it stayed in frame. | By design | Per-SKU QR + continuous scanning; a customer won't re-present the same unit. Discussed per-unit serialization, rejected as overkill for FMCG. Cooldown restored to 1500 ms. |
| 3 | 2026-07-16 | Frontend | 4 | Cart quantity could be raised above the available stock. | By design | Frontend only warns (amber "Only N in stock"); the hard availability check is a backend responsibility — stock is authoritative server-side. Documented in `requirements.md` §10. |
| 2 | 2026-07-16 | Frontend | 4 | Manual add required typing a raw QR string (`POS-QR-000001`) — unusable for an operator. | Fixed | Added a debounced product/variant **search** (`ProductSearch.jsx` + `variantService.search`) feeding the same lookup path. |
| 1 | 2026-07-16 | Frontend | 4 | Closing the camera scan modal left the camera light on (stream not released) until a full page refresh. | Fixed | Unconditional `teardown()` in `CameraScanner.jsx` (StrictMode's remount race left `isScanning` unset on the orphaned camera, so the guarded stop skipped it). |

## Code smells fixed

Not runtime bugs — duplication / inconsistency caught during review that would compound as the app grows. Logged so future-project prompts can pre-empt the same patterns. Two recurring themes so far: **the second copy is the moment to extract; by the third it's already drift**, and **a comment explaining why something odd is necessary is a claim to check, not a reason to keep it**.

| # | Date | Area | Phase | Smell | Fix |
|---|------|------|-------|-------|-----|
| C4 | 2026-08-04 | Backend | C2 | `SchemaConstraintsIT` injected an `EntityManagerFactory` it never used, with a javadoc explaining it was needed "to force the entity manager factory to start, which is what runs the `create-drop` DDL". An IDE unused-field warning flagged it; the obvious resolutions were both wrong — suppress the warning, or keep the field because the comment justified it. | **The comment was false, and checking took one command.** Deleting the field and re-running left the suite green: refreshing a Spring context eagerly instantiates every singleton, so the factory had already started and the schema already existed. Suppressing would have preserved a comment that misdescribes how the test works — worse than the warning. Field removed, the real precondition stated on the `DataSource` instead. Its *intent* (ensure the schema exists) was replaced with `createsEveryTable`, which asserts all nine tables are present and so separates "an entity was never scanned" from "a constraint was never declared" — previously the former surfaced as `uk_variant_tenant_sku does not exist`, pointing at the wrong thing. Mutation-checked. `backend/src/test/java/com/pos/pojo/SchemaConstraintsIT.java`. |
| C3 | 2026-08-01 | Frontend | 7 | Bare-text empty states (`"No products found…"`, `"No items yet…"`) repeated per screen, and conflating *nothing-exists-yet* with *filters-matched-nothing* (Products.jsx hinted "Try clearing your filters" but wired up no button). | Shared `components/EmptyState.jsx` with two shapes — a CTA link (nothing-yet) vs a working action button (filtered-to-zero, e.g. "Clear filters"). Applied to Products + all Phase 7 lists from the start. |
| C2 | 2026-08-01 | Frontend | 7 | Inline status-pill class ternaries (`Products.jsx`'s Active/Inactive `rounded bg-…-100 text-…-700`) about to be re-invented for order status, role, and user status across three new screens. | Shared `components/StatusBadge.jsx` with a `tone` → class map (good/neutral/warn/bad/info). One badge looks the same for the same meaning everywhere. |
| C1 | 2026-08-01 | Frontend | 7 | `METHOD_LABELS` (CASH→"Cash", …) copy-pasted in **three** files (`Payment.jsx`, `ReturnForm.jsx`, `CreditNoteDocument.jsx`); a fourth was about to be added in Return history. | Centralised as `PAYMENT_METHOD_LABELS` in `domain/constants.js` (next to the `PAYMENT_METHODS` enum it labels); all four sites import it. The enum held only codes, so there was genuinely nothing to import before — the label map belonged in the same module. |
